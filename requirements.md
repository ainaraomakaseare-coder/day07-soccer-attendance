# サッカー出欠アプリ NEO 要件定義

DAY9。DAY7 で作った出欠管理を、インターネットに公開して実際のチームで使える形にする。

## これは何か

FCあかつき **1チーム専用**の出欠管理。インターネットに置くが、**鍵を持つ人だけが中を見られる**。

## 誰が使うか

| | 人数 | 端末 | 入口 |
|---|---|---|---|
| メンバー | 〜30人 | スマホ | チームLINEに流れてくるリンク |
| マネージャー | 1人 | スマホ / PC | 管理用リンク |

## 公開範囲の方針

URL は誰でも開ける。**しかし鍵が無いと1行も見えない。**他のチームは受け入れない。

この線引きの結果、**利用規約・プライバシーポリシー・問い合わせ窓口は不要**になる。他人のデータを預からないため。

---

## 今回やること

### A. ログイン

今はリンクそのものが鍵で、ずっと有効。転送された相手がなりすませる。
これを**一度きりの招待状**に変える。

**移行のしかた（実装時に決めた）**：まだログインしていない人の招待リンクは、今までどおり使える
ままにする。全員が一斉にログインするのを待たずに移行できるようにするため。ログインした人から順に、
その人のリンクが無効になっていく。マネージャーは管理画面で誰がログイン済みかを見られる。

1. メンバーがLINEの招待リンクを開く
2. 「Googleでログイン」を押す
3. その Google アカウントと名簿の本人が結びつく
4. **以後は素のURLを開くだけ**で自分の画面が出る（Supabase のセッションは既定で無期限）
5. 紐付け済みの招待リンクは**無効**になる

LINE の内蔵ブラウザは Google 認証を弾く（`403: disallowed_useragent`）ため、配布リンクの末尾に
`?openExternalBrowser=1` を付け、端末の既定ブラウザで開かせる。

**ライブラリは使わず、素の fetch で書く。** このアプリの方針（DAY6・DAY7 から継続）に合わせる。
Supabase の `/auth/v1/authorize` に飛ばし、戻りの URL 断片からトークンを受け取り、
期限が来たら `/auth/v1/token` で更新する。

### B. 自分の表示名を変える

マネージャーが登録した名前を、本人が変更できるようにする。実名で運用する前提のため、
表記のゆれ（「山本 陸」／「山本陸」）を本人が直せる必要がある。

### C. 管理トークンの再発行

今は漏れても作り直す手段が無い。Supabase の SQL Editor を開くしかない。
管理画面のボタンから作り直せるようにする。

### D. 画面の作り直し

Anthropic 公式 Skill `frontend-design` を使って、メンバー画面と管理画面を作り直す。
Before / After を同条件のスクリーンショットで並べられるようにする。

### E. 公開

`npm run build` で公開してよいファイルだけを取り出し、Netlify に置く。
接続情報は Netlify の環境変数に入れ、**リポジトリには入れない。**

---

## やらないこと

線引きをはっきりさせる。以下は今回の対象外。

- **他チームの受け入れ**（＝サービス化）。表4つ・関数14本の作り直しになる。別日にやる
- 出欠の選択肢を増やす（未定・遅刻）。DAY7 から据え置き
- 回答の締切
- 通知（LINE・メールの送信）
- 出席率の集計
- 予定の繰り返し登録

---

## データの変更

**既存データは消さない。追記だけ。**

| 変更 | 内容 |
|---|---|
| `members.auth_user_id` | 1列追加（uuid・unique・null可）。Google アカウントとの紐付け先 |
| `members.token` | 残す。役割が「恒久的な鍵」から「一度きりの招待状」に変わる |
| 関数の追加 | 紐付け・自分の名前変更・管理トークン再発行 |
| 関数の変更 | メンバー用の窓口を、トークン照合から `auth.uid()` 照合に切り替え |

RLS は変えない。テーブルに鍵をかけて関数だけを窓口にする設計は DAY7 のまま。

---

## 人がやる作業（私からはできない）

DAY7 でリポジトリを作れなかったのと同じ構図。

1. **Google Cloud Console** で OAuth クライアントを作る（クライアントID・シークレットを取得）
2. **Supabase の管理画面** → Authentication → Google プロバイダを有効化して貼る
3. **Netlify** にサイトを作り、環境変数に接続情報を入れる

シークレットは Supabase の画面に貼るだけ。リポジトリにも `.env` にも書かない。
この環境から `supabase.com` に通信できないため、私は触れない。

---

## 費用

**0円。**

| 項目 | 金額 | 根拠 |
|---|---|---|
| Google OAuth | 0円 | 無料 |
| Supabase Google プロバイダ | 0円 | 無料枠の月間アクティブユーザーは 50,000 |
| Netlify | 0円 | 無料枠。帯域 100GB/月 |
| 独自ドメイン | 0円 | 使わない。`○○○.netlify.app` のまま |

Supabase 無料枠は**1週間アクセスが無いとプロジェクトが一時停止**する。
データは消えず管理画面から戻せるが、オフシーズンは注意。

---

## 出来上がりの判定

- [x] Before / After のスクリーンショットが同条件で並ぶ
- [x] DAY7 のテスト34項目が通る（＋今回の追加分）→ 42項目すべて合格
- [ ] 招待リンクを開いてログインし、閉じて開き直しても自分の画面が出る
      ← Google 側の設定が済んでから確認する
- [x] 紐付け済みの招待リンクを他人が開いても入れない（DBテストで確認）
- [x] 公開した URL を別の端末で開いて動く → https://lucent-taffy-fd93c5.netlify.app
- [x] リポジトリに接続情報が入っていない（コミット前後に全文検索で確認）
