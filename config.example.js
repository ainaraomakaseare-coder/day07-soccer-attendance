// 接続情報の見本。これは GitHub に上がります（値は空です）。
//
// 本物の値が入った config.local.js は npm run setup が .env から自動生成します。
// config.local.js は .gitignore 済みなので GitHub には上がりません。
window.APP_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
